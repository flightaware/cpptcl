name: Mac CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        brew update
        brew install tcl-tk || true
        mkdir -p /usr/local; ln -s /usr/local/opt/tcl-tk/include /usr/local/include/tcl8.6
        cp /usr/local/opt/tcl-tk/lib/libtcl* /usr/local/lib
        ln -s /usr/local/opt/tcl-tk/bin/tclsh8.6 /usr/local/bin/tclsh
        ln -s /usr/local/opt/tcl-tk/bin/tclsh8.6 /usr/local/bin/tclsh8.6
    - name: make
      run: make
    - name: install
      run: sudo make install
    - name: make test
      run: make test
    - name: make examples
      run: make examples
